# T04 preimplementation QA

QA permits branch1 under the explicit outcome-driven override: Follow the finding,
not this paragraph, if they differ. User already signed both branches. Literal
sample timing assertion is unsupported; truthful annotation preserves T01 finding.
No new approval required, no attempt spent before implementation.

1. Insert only immediately after predecessor T01 Accept fence; preserve all other
bytes including T03 amendment, other four Accepts and guide.
2. Keep existing four-line For humans command byte-for-byte unchanged.
3. Add dated ACCEPT ALTERED AFTER APPROVAL marker, immediately after fence.
4. Compare human-approved record Para humanos vs current/both executions For humans;
attempt1 divergence predates objection round.
5. Approval-time bytes/originalcommand, editor/time and cause remain unknown;
no event assertion of change during execution or unrecorded sign-off violation.
6. Cite deviations.md, T01-gates.json/attempt1output and T01-attempt2-gate-04.txt.
7. Keep English header under human decision; documentary DEVIATION A only.
8. Report branch and quote found Accept in implementation evidence.
9. No new tests; inspect exact placement/scope/facts before mechanical gates.

Recommended annotation wording (source citations may be added):

> ACCEPT ALTERED AFTER APPROVAL (recorded 2026-09-08 under
> manual-loops/architecture/blocker-handoff-certification.md): the human-approved
> record says this block read `grep -n "Para humanos"`, while the current block
> and both recorded executions read `grep -n "For humans"`. The predecessor
> evidence has no approval-time SPEC copy or original command, editor identity,
> or edit time, and does not establish whether the runner substituted the command
> or the SPEC changed during execution. The current English command matches the
> guide and is left in place under the human decision accepting that header. What
> is corrected here is the record: this divergence is DEVIATION A in the
> certification evidence.

Risks: sample contradicts no-reconstruction; outcome-driven clause resolves it.
Grep Accept does not prove command preservation/placement; diff review required.
No QA edits/gates/Accept. Standard3gates plus T04Accept and required predecessor
T01Accept must run verbatim, all through the mechanical executor.
