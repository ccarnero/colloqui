# T03 preimplementation QA

QA_READY_FOR_IMPLEMENTATION; no blocker. Native fp-qa Sol/medium provenance:
T03-preflight-provenance.json.

1. Replace only the two Progress template paragraphs; preserve bytes before the
first paragraph and from Out of scope onward, including later Preflight claims.
2. Preserve Progress heading and all five checked task boxes in order.
3. Add the exact dated EVIDENCE AMENDMENT heading and evidence-only review caveat.
4. Record T01-T05 actual commands/statuses/native durations, hashes, provenance,
preservation and QA from packet sources without recomputation.
5. Preserve T01 two attempts, evidence-only objection, A APPROVED/B REJECTED,
missing final attempt-2 verdict, and completion-timestamp caveat.
6. Deviation A cause/editor/timing remain uncertain; do not pre-decide T04.
7. Preserve every historical hole as not recorded, including verdicts, missing
preservation files, runner identities, dev/QA/reviewer turns and approval history.
8. Explain no cluster/dev-mode gates; distinguish current G19 integration finding;
record old/new dev hashes, other five unchanged entries and fresh-session effect.
9. Preserve rejected820 defect, first reviewer block, four integration commits and
human composition finding, separate from deviations A/B.
10. Only integrated9fde is certified later; cite packet, evidence directory,
T02-attempt2-review-verdicts.md and current SPEC. b9764a6a is certification commit.
11. Verify against packet64384e65 and source records; never round native values.
12. Check exact Accept plus stronger boundary/checkbox/complete-record invariants.

Risks: out-of-scope historical Preflight claims must remain verbatim but be
qualified; current reviews do not restore original missing verdicts. Grep Accept
alone does not establish complete evidence or preserved boundaries. Markdown-only:
no tests applicable; no QA edits, gates or Accept execution.

Non-causal principal evidence extraction retry: an attempted native lookup of the
QA inter-agent message found no corresponding event and stopped at an assertion.
No file was written. This plan summarizes the delivered QA message instead;
actual model/run/turn provenance was extracted successfully and independently.
