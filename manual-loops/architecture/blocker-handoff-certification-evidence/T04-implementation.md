# T04 implementation

Branch1, selected because current T01 command reads For humans. Only a13-line
annotation immediately after T01 Accept fence was inserted; all original file
bytes remain unchanged. Outcome-driven clause follows T01 uncertainty rather
than contradictory sample timing. Source citations and unknown editor/time/cause
are explicit; English command/header stay under human approval.
File SHA-256 d896e6954b4d43249ea477789e04848cc9caf09045e2d26eb4395a76f5a89cd4.
Actual native Sol/medium run/turn in T04-preflight-provenance.json.
Other four Accepts match their recorded commands; no follow-up finding.
No tests applicable, gates/Accept/commit, scope deviation, blocker or fallback.
Non-causal coordination: two early QA-plan reads found it not yet written;
developer waited, read it fully after principal confirmation, then edited once.

Found and retained Accept verbatim (also required current gate):

```sh
grep -n "HANDOFF NOTE" DOCS/guides/agent-roles.md && \
grep -n "consumes no attempt" DOCS/guides/agent-roles.md && \
grep -c "do not diagnose, remediate, edit, retry" DOCS/guides/agent-roles.md
```

Execution order: three standard gates, this required predecessor Accept, then
certification T04's own Accept. No T02-only clean-worktree gates apply.
