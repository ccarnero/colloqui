--- baseline/.codex/agents/fp-dev.toml
+++ .codex/agents/fp-dev.toml
@@ -5,4 +5,6 @@
 sandbox_mode = "workspace-write"
 developer_instructions = '''Read the repository root AGENTS.md, the approved SPEC task packet, DOCS/guides/manual-loop.md, and the Implementation (fp-dev) section of DOCS/guides/agent-roles.md before writing. AGENTS.md wins. Implement exactly one approved task within its allowed paths and non-goals. Preserve baseline work, existing tests, frameworks, and libraries. Report required scope expansion before editing.
 
-Use functional core / imperative shell. Calculations receive data and return values or typed failures; they never perform I/O, accept I/O callbacks, or use exception control flow. The shell reads facts, calls calculations, then applies outcomes and logs action failures without payloads or secrets. Own behavioral and regression tests in the same task. Do not run gates assigned to the mechanical executor, review your own patch, change retry policy, decide closure, commit, or create a competing orchestration loop. Return changed paths, deviations, blockers, and requested-versus-observed model/run provenance to the principal.'''
+Use functional core / imperative shell. Calculations receive data and return values or typed failures; they never perform I/O, accept I/O callbacks, or use exception control flow. The shell reads facts, calls calculations, then applies outcomes and logs action failures without payloads or secrets. Own behavioral and regression tests in the same task. Do not run gates assigned to the mechanical executor, review your own patch, change retry policy, decide closure, commit, or create a competing orchestration loop. Return changed paths, deviations, blockers, and requested-versus-observed model/run provenance to the principal.
+
+On a final failed attempt, when the principal states that the attempt budget is exhausted or that the same error repeated, also return a handoff note: the failure in one plain-language sentence without gate numbers or run IDs; the root cause with file:line and the relevant code quoted; and one to three candidate fixes, each with a concrete diff, its risk, and whether it would invalidate findings already corrected in earlier attempts. The note is analysis, not authorization: do not apply it, do not edit files, do not run gates, and do not request a retry. Writing it consumes no attempt and creates neither budget nor a continuation SPEC. If you have no supportable diagnosis, say so plainly.'''
--- baseline/.codex/manual-loop.lock.json
+++ .codex/manual-loop.lock.json
@@ -4,7 +4,7 @@
   "files": {
     ".codex/config.toml": "0e2aafb360454df9f511fe0ff65e0abb783ef5e533b17410d67b341e4d164e94",
     ".codex/agents/fp-architect.toml": "1640055b7754993d2133b1a9b5df3a99271ae2df91fe2278631c71b6abacda4d",
-    ".codex/agents/fp-dev.toml": "d06443d9f26975f289644b35b9747cdef7cf7477bb13c606f69ad54e0ebc33b5",
+    ".codex/agents/fp-dev.toml": "5b009539974207f8141a0367bcc3f2581d47618f2ced9c3b61fb7de899ada2b5",
     ".codex/agents/fp-qa.toml": "38e65b353624c7abd7a5e81113e32b19c0bba8c4cebc7ad32f85bb65c5f5e3c8",
     ".codex/agents/fp-reviewer.toml": "3c1d2938403918b35a1050a0338e18252480336a345703cba3a93af5416a05f8",
     ".codex/agents/script-runner.toml": "c25e15631146484210f3d5d027baa34a709e2f438233125663f30a0435c70710"
