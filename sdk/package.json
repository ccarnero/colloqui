{
  "name": "@yoizen/http-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Plain-Node ESM SDK for sending (ingesting) messages into the platform via the http channel.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
