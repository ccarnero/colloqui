scenarios:
  - name: "events-publish"
    flow:
      - function: "ensureAuthContext"
      - loop:
          - function: "setEventRequest"
          - post:
              url: "/api/events"
              beforeRequest: "setGatewayAuthHeaders"
              afterResponse: "captureEventId"
              body: "{{ eventPayloadJson }}"
        count: "{{ $processEnvironment.ARTILLERY_EVENTS_PER_VU }}"
