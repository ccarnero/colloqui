{
  "name": "stress-tests",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Black-box end-to-end stress tests (k6 + Bun sink + reconciler).",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "npx @biomejs/biome check lib scenarios sink reconcile",
    "lint:fix": "npx @biomejs/biome check --write lib scenarios sink reconcile",
    "sink:dev": "bun run sink/server.ts",
    "sink:build": "./sink/build-and-load.sh",
    "reconcile": "bun run reconcile/reconcile.ts",
    "provision": "./scripts/provision.sh",
    "scenario:webhook": "./scripts/run.sh --scenario webhook-ingress"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@types/bun": "^1.1.13",
    "@types/k6": "^1.3.1",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
