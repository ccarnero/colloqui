apiVersion: v1
kind: Service
metadata:
  name: searxng-mcp
  namespace: platform-services-dev
  labels:
    app: searxng-mcp
spec:
  selector:
    app: searxng-mcp
  ports:
  - port: 32769
    targetPort: 32769
    name: sse
  type: ClusterIP
