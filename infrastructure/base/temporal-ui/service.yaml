apiVersion: v1
kind: Service
metadata:
  name: temporal-ui
  labels:
    app.kubernetes.io/name: temporal-ui
    app.kubernetes.io/component: workflow-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: temporal-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
