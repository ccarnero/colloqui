apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespaced entrypoint for local dev bootstrap with `--storage-engine=mongo`.
# Sibling of `overlays/mongo/` to avoid kustomize parent/child cycles.

namespace: support-services-dev

resources:
  - ../mongo
