apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: acme-dev-ns

resources:
  - namespace.yaml
  - ../../../base
  - endpoint-slice.yaml

patches:
  - path: env-patch.yaml
  - path: resources-patch.yaml
  - path: service-patch.yaml
  - path: source-path-patch.yaml
