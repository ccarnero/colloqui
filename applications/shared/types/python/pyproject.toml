[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "yoizen-shared-types"
version = "0.1.0"
description = "Shared types for Yoizen platform"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["subjects", "envelope", "nats_helpers", "cloudevent_envelope"]
