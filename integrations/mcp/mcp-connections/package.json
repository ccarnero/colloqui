{
  "name": "mcp-connections-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "mcp-connections sample driver — verifies the manifest-provisioned MCP server, agent, and workflow (auth, test-connection, tool discovery, per-tool agent enablement, and an mcpCall workflow action) through @yoizen/platform-sdk.",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@yoizen/platform-sdk": "file:../../../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.23.0",
    "typescript": "^5.7.2"
  }
}
