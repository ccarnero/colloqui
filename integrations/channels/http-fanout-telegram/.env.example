# Copy to .env and fill in. Loaded automatically by run.sh.
# Provisioning is declarative now (manifest.yaml + `yoizen manifests apply`)
# — the Telegram recipient chat id lives in the manifest's `systemVariables`
# section, not here. See README.md.
# Infra (endpoint/host/tenant/creds) is auto-resolved — only overrides go here.

# OPTIONAL: overrides for run.sh's exercise driver (defaults match manifest.yaml).
# FANOUT_WORKFLOW_NAME=http-fanout-telegram
# FANOUT_HTTP_EXTERNAL_ID=manifest:http-fanout-telegram
# RUN_TEXT=hola desde run.sh
