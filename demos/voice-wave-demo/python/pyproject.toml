[project]
name = "voice-wave-demo-worker"
version = "0.1.0"
description = "Python sidecar that runs Supertonic TTS for the voice-wave-demo service"
requires-python = ">=3.10"
dependencies = [
  "supertonic>=0.1.0",
  "numpy>=1.26",
  "soundfile>=0.12",
]

[tool.uv]
package = false
