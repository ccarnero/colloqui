# Strategic-merge patches shared by staging overlays (local + cloud).
#
# Same `min-scale: 0` shape as `scale-to-zero-non-prod`; the difference
# lives in the sibling `keda-scale-to-zero.yaml` which uses a longer
# `cooldownPeriod: 300` to absorb overlapping canary releases without
# pod churn.
#
# `api-gateway` is intentionally excluded — entry point for devs +
# automation; cold-starting it on every poke kills DX.
#
# `initial-scale: 0` is intentionally NOT set: vanilla Knative gates
# zero-initial-scale behind the cluster-wide flag
# `allow-zero-initial-scale=true` (config-autoscaler ConfigMap),
# which our minikube/orbstack/cloud clusters do not enable.
#
# Services NOT included here (kept at `min-scale: 1` from base) —
# all of them embed an internal NATS durable consumer that must
# stay alive to drain its queue:
#   • `audit-service`, `event-processor`, `metrics-service`,
#     `webhook-service`, `workflow-api`, `channel-service`,
#     `usage-aggregator-service`.
# Splitting the consumer side off into a dedicated
# `apps/v1.Deployment` + KEDA scaler is tracked as follow-up.
#
# `workflow-worker` and `workflow-http-worker` are plain
# `apps/v1.Deployment` resources and are NOT patched here — see
# `keda-scale-to-zero.yaml` for the staging override on their
# ScaledObjects.
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: auth-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: cache-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: tenant-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: scheduler-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: proxy-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: registry-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: adapter-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: admin-console
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: messaging-console
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
---
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: yoizenclaw-admin-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/scale-down-delay: "30s"
        autoscaling.knative.dev/window: "60s"
