apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Backward-compatible alias for `local/postgres-dev` (default Postgres engine).
# Prefer: `./bootstrap-orbstack-osx.sh dev platform-services` (uses postgres-dev)
# or explicit `local/postgres-dev` / `local/mongo-dev` with `--storage-engine`.

resources:
  - ../postgres-dev
